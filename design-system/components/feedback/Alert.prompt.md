Inline message / toast banner in four tones.

```jsx
<Alert tone="success" dismissible>Report generated successfully.</Alert>
<Alert tone="error">Invalid CVSS vector.</Alert>
```

`tone`: `success` | `error` | `warning` | `info`. Add `dismissible` (or pass `onClose`) for the × button.
